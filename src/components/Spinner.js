import { View, Text } from 'react-native'
import React from 'react'

const Spinner = () => {
    return (
        <View>
            <Text>Spinner</Text>
        </View>
    )
}

export default Spinner